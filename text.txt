mysql --version
mysql -u root -p
show databases;
create database mahimadb;
use mahima; // change to mahima database
CREATE TABLE EMPLOYEE(
    EmpID int,
    FirstName varchar(255),
    LastName varchar(255),
    EmpAge int,
    EmpZone varchar(255),
);

// DESC Emp;

//Insert into Employee(EmpID,FirstName,LastName,EmpAge, EmpZone) Values(1,'ankita','bhardwaj'
23,'north-east');

Select * from Employee;// to see all data of the table
Select lastname from Employee;// last name from Employee
Select EmpAge, EmpZone from Employee;


Select  Distinct EmpAge from Employee;
Select * from Employee;
Where EmpAge=25;
where empAge>30;

Select * from Employee;
Where EmpAge=25;
where empAge>30;
 Create Table EMPLOYEE(;
FirstName varchar(255),
LastName varchar(255),
EmpId int NOT NULL,
EmpAge int,
EmpZone varchar(255)
 Primary Key (EmpId)
);

CREATE TABLE Employees (
    FirstName VARCHAR(255),
    LastName VARCHAR(255),
    EmpAge INT,
    EmpZone VARCHAR(255),
    EmpId INT NOT NULL
);


CREATE TABLE EMPL(
    FirstName varchar(300),
    LastName varchar(355),
    EmpAGE INT,
    EmpZone  VARCHAR(300),
    EMPID INT NOT NULL
)

CREATE TABLE EMPLL(
    FirstName varchar(300),
    LastName varchar(355),
    EMPID  INT,
    EMPZONE VARCHAR(400),
    EMPDEPT  VARCHAR(300) Default 'Operations'
);

create table emplloyee(
    firstname varchar(400),
    lastname varchar(300),
    empid int,
    empzone varchar(400),
    empdept varchar(400),
    empage int ,
    check(empage>20)
);
 Alter Table Emplloyee
 Add EmpSubject varchar(300);

 Alter Table Emplloyee
 Drop  Column EmpSubject ;

 Alter Table Emplloyee
 Alter column Empdept int;

 select firstname, lastname from emplloyee;
 select firstname, lastname from emplloyee limit 5;
 select Distinct empage from emplloyee;

 Select * from Emplloyee 
 where Empage>=23;

 Select * from Emplloyee
 where Empage> 20;

 Select * from Emplloyee
 where Empage Between 20 and 23;

 Select * from Emplloyee
 where EmpID=2;

 Select empage from Emplloyee
 where empid is not null;

 select empid from emplloyee;
 select Max(EmpId) from emplloyee;

 Select Min(EmpId) from emplloyee;
 Select EmpAge , AVG(EmpId)  from emplloyee;

 Select Empdept from Emplloyee
 where EmpSubject='English'

Alter Table Emplloyee
Drop Column EmpAGE;

Desc Emplloyee;

Alter Table Emplloyee
Add Column EmpDept varchar(255);

Alter Table Emplloyee
Add Column  EmpDept varchar(255);

Desc Employee;

Alter Table Emplloyee
Alter Column EmpDept int;

Select  EmpDept, Empid As number_of_listings
from Emplloyee
Group By EmpAge;
show tables

Select Table
use mahimadb

create table Employee(
empid int not null
firstname varchar(255)
lastname varchar(255)
unique (empid)
);

Create Table Employee(
    EmpId  int not null,
    firstname varchar,
    lastname varchar,
    empdept varchar(355) default 'operations'
);
create table employee(
    firstname varchar(244),
    lastname varchar(244),
    empid varchar(200),
    empdept varchar(300)

)
Insert into employee(firstname, lastname, empid, empdept) values ('Mahima','Bhardwaj',1,'chemistry')
Insert into employee(firstname, lastname, empid, empdept)values ('zoe', 'sharma', 2,'english');
Insert into employee(firstname, lastname, empid, empdept) values ('kareena', 'kapoor',3,'hindi');
Insert into employee(firstname, lastname , empid, empdept) values ('katrina','kaif', 4, 'maths');

Create Table Empps(
    EmpId int,
    EmpSubject varchar(255),
    firstname  varchar(255),
    lastname varchar(255),
    check (empAge > 20)
);